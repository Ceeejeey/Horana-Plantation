rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Static report assets — uploaded via scripts/upload-storage-assets.mjs
    match /assets/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
